# IDE
.idea

# Python
__pycache__/
*.py[cod]
*$py.class

# local compose runtime data + secrets
.env
# git-ignored secret/local overrides for the compose stack (real TOKEN etc.)
docker-compose.override.yml
docker-compose.override.yaml
# ignore everything under config/ (rconpw, server-id.json, lists, live *.json)
# except the committed reference examples
/config/*
!/config/*.example.json
/saves/
/mods/
/scenarios/
/script-output/
/data/
